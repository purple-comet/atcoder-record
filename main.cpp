// 本物のmain.cppは.config内 acc configから調べられる
#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    cout.setf(ios::unitbuf);
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int N;
    cin >> N; 
    cout << "\n";
    return 0;
}